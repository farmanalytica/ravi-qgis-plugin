#earthengine-api>=0.1.335
#ee-s1-ard>=1.0.5
# Pinned: 3.0.1 pulls geopolars==0.1.0a4 (pre-release, no universal2/manylinux
# wheels) which breaks the --only-binary extlibs build. 3.0.0 is the version
# shipped in the committed bundles and resolves cleanly on all targets.
agrigee-lite==3.0.0